# 背诵xv6的计划

在学习OS的过程中，总是想着要根据所学去写，但是又想要边写边学来实践；本意是好的，但是执行有问题。

学的不透，就写不出来；写不出来，就学不透。一根筋变成两头堵了。

所以我有一个大胆的想法，直接背诵xv6。

xv6的代码量不大，只有一万行，

```bash
wc -l kernel/*.c kernel/*.S user/*.c user/*.S mkfs/*.c 2>/dev/null | tail -1
```

虽然看起来也没有少到可以背。但是哪怕知识把全部的文件名记下来，也比什么都不背要好一些。

所以我将会从记住全部文件夹开始背xv6。
